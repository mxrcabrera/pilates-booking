import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Calendario | Pilates Booking'
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
